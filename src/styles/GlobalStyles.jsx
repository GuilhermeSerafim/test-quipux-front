import { createTheme } from "@mui/material/styles";
import "@fontsource/raleway"; // Importa a fonte Raleway

const theme = createTheme({
  typography: {
    fontFamily: "Raleway, Arial, sans-serif", // Define Raleway como fonte padrão
  },
});

export default theme;
